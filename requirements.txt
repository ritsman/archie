fastapi
uvicorn[standard]
SQLAlchemy
asyncpg           # PostgreSQL async driver
databases          # Optional, if used for async DB layer
pydantic           # Pydantic v2 (note some warnings about config keys change)
psycopg2-binary    # If you use psycopg2 sync driver somewhere
python-multipart   # For form handling if needed
alembic          # For migrations if you run DB migrations
python-dotenv      # For env variables loading if used
fastapi-utils      # Optional, utilities for FastAPI
